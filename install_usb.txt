Problem:
Das USB-Device soll in jedem Fall, unabhängig vom Steckplatz, den gleichen Namen erhalten.

Also z. Bsp.: /dev/ttyMCC
  
#dmseg 
#lsusb 
aufrufen
 
Ausgabe zeigt uns unter welcher VendorID oder SerialNumber das Device sich meldet.

#sudo nano /etc/udev/rules.d/55-usbcom.rules
..
SUBSYSTEM=="tty", ATTRS{idVendor}=="0403", ATTRS{idProduct}=="6001", ATTRS{serial}=="A106K4W0", SYMLINK+="ttyMCC", MODE="0666"
..

Neu laden ohne reboot:

#sudo udevadm control --reload
#sudo udevadm trigger --action=add